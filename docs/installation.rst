============
Installation
============

At the command line::

    $ easy_install django-geonames-data

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-geonames-data
    $ pip install django-geonames-data
